export const config = {
  /**
   * @description Aktifkan atau nonaktifkan pembacaan status WhatsApp secara otomatis.
   * @type {boolean}
   */
  autoReadStory: true,

  /**
   * @description Aktifkan atau nonaktifkan pengiriman reaksi ke status WhatsApp secara otomatis.
   * @description Opsi ini memerlukan `autoReadStory` untuk diatur ke `true`.
   * @type {boolean}
   */
  autoReactStory: false,

  /**
   * @description Emoji yang akan digunakan untuk reaksi status otomatis.
   * @type {string}
   */
  reactEmote: "💜",
};
